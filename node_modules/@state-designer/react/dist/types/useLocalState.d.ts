import { S } from '@state-designer/core';
/**
 * Create and subscribe to a new state.
 * @param design A designuration object for a new state.
 * @param dependencies (optional) An array of dependencies that, when changed, will rebuild a new state from the provided design.
 */
export default function useLocalState<D, R extends Record<string, S.Result<D>>, C extends Record<string, S.Condition<D>>, A extends Record<string, S.Action<D>>, Y extends Record<string, S.Async<D>>, T extends Record<string, S.Time<D>>, V extends Record<string, S.Value<D>>>(design: S.Design<D, R, C, A, Y, T, V>, dependencies?: any[]): S.DesignedState<D, V>;
