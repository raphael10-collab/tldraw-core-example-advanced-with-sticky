import type * as S from './types';
/**
 * Turn a configuration object into a complete state tree, where shortcuts in the configuration
 * API are converted into a consistent structure.
 *
 * @param config A State Designer configuration object.
 */
export declare function getStateTreeFromDesign<D extends unknown, R extends Record<string, S.Result<D>>, C extends Record<string, S.Condition<D>>, A extends Record<string, S.Action<D>>, Y extends Record<string, S.Async<D>>, T extends Record<string, number | S.Time<D>>, V extends Record<string, S.Value<D>>>(config: S.Design<D, R, C, A, Y, T, V>, id: string): S.State<S.DesignedState<any, any>>;
