import type * as S from './types';
/**
 * Test whether a handler object would pass its conditions, given the current data, payload, and result.
 * @param h Handler
 * @param d Data
 * @param p Payload
 * @param r Result
 */
export declare function testEventHandlerConditions<D, P, R>(h: S.EventHandlerObject<D> | S.InitialStateObject<D>, d: D, p: P, r: R): boolean;
